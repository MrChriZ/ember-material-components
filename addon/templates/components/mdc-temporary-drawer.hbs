{{#if hasSpacer}}
  <div class="mdc-temporary-drawer__toolbar-spacer"></div>
{{/if}}
{{#if rtl}}
  <nav class="mdc-drawer__drawer" dir="rtl">
    {{yield (hash list=(component 'mdc-list' class='mdc-drawer__content' tagName='nav'))}}
  </nav>
{{else}}
  <nav class="mdc-drawer__drawer">
    {{yield (hash list=(component 'mdc-list' class='mdc-drawer__content' tagName='nav'))}}
  </nav>
{{/if}}
